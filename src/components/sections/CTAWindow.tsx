import type { SectionProps } from "@/types";
import { cn } from "@/lib/utils";

import Copy from "@/components/ui/Copy";

interface CTAWindowProps extends SectionProps {
    img: string;
    header: string;
    callout: string;
    description: string;
}

export default function CTAWindow({
    img,
    header,
    callout,
    description,
    className,
    id,
}: CTAWindowProps) {
    return (
        
    )
}
